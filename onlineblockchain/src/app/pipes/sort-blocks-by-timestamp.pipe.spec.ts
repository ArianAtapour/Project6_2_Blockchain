import { SortBlocksByTimestampPipe } from './sort-blocks-by-timestamp.pipe';

describe('SortBlocksByTimestampPipePipe', () => {
  it('create an instance', () => {
    const pipe = new SortBlocksByTimestampPipe();
    expect(pipe).toBeTruthy();
  });
});
