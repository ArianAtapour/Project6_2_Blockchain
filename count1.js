// Define a variable to hold the count of counters
var counter1 = 0;
// Function to increment the counter 
function addCounter() {
    counter1++;
    console.log("There are now ".concat(counter1, " counters."));
}
