// Define a variable to hold the count of counters
let counter1 = 0;

// Function to increment the counter 
function addCounter() {
  counter1++;
  console.log(`There are now ${counter1} counters.`);
}

// Call the addCounter function to increment the count
addCounter(); 
addCounter(); 
addCounter(); 