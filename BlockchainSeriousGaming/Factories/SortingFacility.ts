import { Factory } from './Factory';
class SortingFacility {
    name: string;
    id: number;

    constructor(name: string, id: number) {
        this.name = name;
        this.id = id;
    }
}

// const sortingFacility: SortingFacility = new Factory("IDK", 6969);
