import { Factory } from './Factory';
class Client {
    name: string;
    id: number;

    constructor(name: string, id: number) {
        this.name = name;
        this.id = id;
    }
}

// const client: Client = new Factory("Murphy", 1);
