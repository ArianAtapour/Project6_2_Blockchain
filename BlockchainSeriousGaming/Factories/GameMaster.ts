import { Factory } from './Factory';
class GameMaster {
    name: string;
    id: number;

    constructor(name: string, id: number) {
        this.name = name;
        this.id = id;
    }
}

// const gameMaster: GameMaster = new Factory("David", 420);
