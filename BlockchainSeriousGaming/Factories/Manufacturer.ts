import { Factory } from './Factory';
class Manufacturer {
    name: string;
    id: number;

    constructor(name: string, id: number) {
        this.name = name;
        this.id = id;
    }
}

// const manufacturer: Manufacturer = new Factory("Apple", 234324);
